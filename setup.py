from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='before moving ci/cd i do practice all which i learn till now',
    author='UR Rehman',
    license='',
)
